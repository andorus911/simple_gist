<% if !current_user.nil? %>
  <% if current_user.admin? || current_user?(object.user) %>
    <%= link_to "delete", object, method: :delete, data: { confirm: "You sure?" }, title: object.content, id: "delete" %>
  <% end %>
<% end %>